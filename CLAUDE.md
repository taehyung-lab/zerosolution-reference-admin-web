@AGENTS.md

# Claude Code

Load only the project skill routed by `AGENTS.md` for the confirmed edit scope. Project skills live at `.agents/skills/{skill-name}/SKILL.md` and are read as files; do not create runtime-specific skill copies.
