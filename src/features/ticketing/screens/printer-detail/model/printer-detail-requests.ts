/**
 * 스마트프린터 조회 화면의 업무 요청 경계다. 원문 「스마트프린터정보를 조회할 수 있다」의
 * `삭제 버튼 → Case02. 대기/완료 중인 경우 클릭시, 삭제 확인 alert 제공 → 삭제하시겠습니까?` 까지가
 * 확정된 사실이고 실제 삭제 API 는 없다(AGENTS 1절).
 * 도달 조건: 조회 화면의 삭제 확인 alert 에서 `확인`. 삭제 완료 alert·목록 복귀·캐시 무효화는
 * 서버 계약이 확정된 뒤 mutation workflow 로 교체하며 여기서 성공을 만들지 않는다.
 *
 * 원문 Case01 `인쇄 중인 경우 삭제 불가 alert` 는 인쇄 상태를 조회 응답에서 읽을 수 있을 때만
 * 판정할 수 있는데 조회 frame·DTO 어디에도 그 값이 없어 이 화면에서는 보류한다(미확인).
 */
export const requestPrinterDelete: (printerId: string) => void = () => {
  console.log('[시나리오] 스마트프린터 삭제: 요청 입력 확인 → API 연결 대기');
};
