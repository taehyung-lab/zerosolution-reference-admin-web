import type { DeepKeysOfType, DeepValue } from '@tanstack/react-form'
import { FormField, type FieldForm } from './FormField'
import { Combobox, type ComboboxOption } from '../primitives/Combobox'

export function FormComboboxField<TValues, TName extends DeepKeysOfType<TValues, string>>({ form, name, label, options, searchValue, onSearchValueChange, placeholder, searchLabel, emptyLabel, required, description }: { readonly form: FieldForm<TValues>; readonly name: TName; readonly label: string; readonly options: readonly ComboboxOption[]; readonly searchValue: string; readonly onSearchValueChange: (value: string) => void; readonly placeholder: string; readonly searchLabel: string; readonly emptyLabel: string; readonly required?: boolean; readonly description?: string }) {
  return <FormField form={form} name={name} label={label} required={required} description={description}>{(field, control, labelId) => <Combobox id={control.id} ariaDescribedby={control['aria-describedby']} ariaInvalid={control['aria-invalid']} ariaLabelledby={labelId} value={typeof field.state.value === 'string' && field.state.value !== '' ? field.state.value : null} onValueChange={(value) => field.handleChange((value ?? '') as DeepValue<TValues, TName>)} onBlur={field.handleBlur} options={options} searchValue={searchValue} onSearchValueChange={onSearchValueChange} placeholder={placeholder} searchLabel={searchLabel} emptyLabel={emptyLabel} />}</FormField>
}
